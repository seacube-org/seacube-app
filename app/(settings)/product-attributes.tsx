import SettingPlaceholder from "@/components/modules/settings/SettingPlaceholder";

// Native placeholder — the full product-attribute catalog UI is web-only (product-attributes.web.tsx).
export default function ProductAttributesSettings() {
  return <SettingPlaceholder titleKey="settings.productAttributes" defaultValue="规格属性" />;
}
